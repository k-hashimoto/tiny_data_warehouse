/** Maximum number of rows returned by a query before truncation. */
export const RESULT_ROW_LIMIT = 1000;

/** Default number of rows per page in result tables. */
export const PAGE_SIZE = 100;

/** Maximum number of editor tabs. */
export const MAX_TABS = 10;
